'use client';
import { Info, Play } from 'lucide-react';
import { imageUrl } from '@/services/tmdb';
import type { Movie } from '@/types';
export function Banner({ movie, onMoreInfo }: { movie: Movie; onMoreInfo: () => void }) { const backdrop = imageUrl(movie.backdrop_path, 'original'); return <section id="top" className="relative flex min-h-[590px] items-center bg-cover bg-center md:min-h-[680px]" style={{ backgroundImage: `linear-gradient(90deg, rgba(0,0,0,.8) 0%, rgba(0,0,0,.25) 55%, transparent 90%), url(${backdrop})` }}><div className="absolute inset-x-0 bottom-0 h-48 bg-gradient-to-t from-[#141414] via-[#141414]/70 to-transparent" /><div className="relative z-10 max-w-xl px-5 pt-20 md:ml-7 md:px-10"><p className="mb-4 text-sm font-semibold tracking-[.25em] text-zinc-200">N SERIES</p><h1 className="mb-5 text-4xl font-extrabold drop-shadow-lg md:text-6xl">{movie.title || movie.name}</h1><p className="line-clamp-3 mb-6 max-w-lg text-sm leading-relaxed text-zinc-100 md:text-lg">{movie.overview}</p><div className="flex gap-3"><button className="flex items-center gap-2 rounded bg-white px-5 py-2.5 font-bold text-black transition hover:bg-white/80"><Play size={22} fill="currentColor" />Play</button><button onClick={onMoreInfo} className="flex items-center gap-2 rounded bg-zinc-500/70 px-5 py-2.5 font-bold transition hover:bg-zinc-500/50"><Info size={22} />More Info</button></div></div></section>; }
