'use client';
import { imageUrl } from '@/services/tmdb';
import type { Movie } from '@/types';
export function MovieCard({ movie, onSelect, tall = false }: { movie: Movie; onSelect: (movie: Movie) => void; tall?: boolean }) { const poster = imageUrl(movie.poster_path, 'w500'); const backdrop = imageUrl(movie.backdrop_path, 'w500'); return <button onClick={() => onSelect(movie)} aria-label={`View ${movie.title}`} className={`group relative shrink-0 overflow-hidden rounded-sm bg-zinc-800 text-left shadow-black transition duration-200 hover:z-10 hover:scale-110 hover:shadow-2xl ${tall ? 'w-[115px] sm:w-[145px]' : 'w-[170px] sm:w-[240px]'}`}><div className={tall ? 'aspect-[2/3]' : 'aspect-video'} style={{ backgroundImage: `linear-gradient(135deg, #242424, #111), url(${tall ? poster || backdrop : backdrop || poster})`, backgroundSize: 'cover', backgroundPosition: 'center' }} /><div className="absolute inset-x-0 bottom-0 translate-y-full bg-zinc-900/95 px-2 py-2 text-xs opacity-0 transition duration-200 group-hover:translate-y-0 group-hover:opacity-100"><p className="truncate font-semibold">{movie.title || movie.name}</p><p className="mt-1 text-green-400">{Math.round(movie.vote_average * 10)}% Match</p></div></button>; }
