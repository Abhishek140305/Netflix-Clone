'use client';
import { ChevronLeft, ChevronRight } from 'lucide-react';
import { useRef } from 'react';
import type { Movie } from '@/types';
import { MovieCard } from './MovieCard';
export function Row({ title, movies, onSelect, tall = false, id }: { title: string; movies: Movie[]; onSelect: (movie: Movie) => void; tall?: boolean; id?: string }) { const ref = useRef<HTMLDivElement>(null); const move = (direction: number) => ref.current?.scrollBy({ left: direction * ref.current.clientWidth * .8, behavior: 'smooth' }); return <section id={id} className="group/row relative mb-8 px-4 md:px-12"><h2 className="mb-3 text-lg font-bold md:text-xl">{title}</h2><button onClick={() => move(-1)} aria-label="Previous titles" className="absolute bottom-0 left-0 top-8 z-10 hidden w-10 items-center justify-center bg-black/60 opacity-0 transition group-hover/row:flex group-hover/row:opacity-100"><ChevronLeft /></button><div ref={ref} className="hide-scrollbar flex gap-2 overflow-x-auto overflow-y-visible pb-9 pt-1">{movies.map((movie, index) => <MovieCard key={`${movie.id}-${index}`} movie={movie} onSelect={onSelect} tall={tall} />)}</div><button onClick={() => move(1)} aria-label="Next titles" className="absolute bottom-0 right-0 top-8 z-10 hidden w-10 items-center justify-center bg-black/60 opacity-0 transition group-hover/row:flex group-hover/row:opacity-100"><ChevronRight /></button></section>; }
